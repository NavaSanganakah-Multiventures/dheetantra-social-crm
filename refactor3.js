const fs = require('fs'); let c = fs.readFileSync('app/dashboard/components/PlivoSettingsSection.tsx', 'utf8'); c = c.replace('export function PlivoSettingsSection', 'interface Agent { userId: string; name?: string; email?: string; voiceStatus?: string; phoneMasked?: string; }\ninterface PlivoConfig { id: string; name: string; authId: string; authTokenMasked: string; isActive: boolean; autoDialAgents: boolean; endpointConfigured: boolean; endpointUsername?: string; endpointPasswordMasked?: string; fromNumbers?: any[]; }\n\nexport function PlivoSettingsSection'); c = c.replace('useState<any>(null)', 'useState<any>(null)'); c = c.replace('useState<any[]>([])', 'useState<Agent[]>([])'); c = c.replace('useState<any[]>([])', 'useState<PlivoConfig[]>([])'); c = c.replace('const [configs, setConfigs] = useState<any[]>([])', 'const [configs, setConfigs] = useState<PlivoConfig[]>([])'); fs.writeFileSync('app/dashboard/components/PlivoSettingsSection.tsx', c);
